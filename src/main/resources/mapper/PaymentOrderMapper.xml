<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lynn.papertrail.mapper.PaymentOrderMapper">

    <select id="selectByOutTradeNo" resultType="com.lynn.papertrail.entity.PaymentOrder">
        SELECT * FROM payment_order WHERE out_trade_no = #{outTradeNo}
    </select>

    <select id="selectByTradeNo" resultType="com.lynn.papertrail.entity.PaymentOrder">
        SELECT * FROM payment_order WHERE trade_no = #{tradeNo}
    </select>
</mapper>
